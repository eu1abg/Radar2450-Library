Radar2450 Library
Расширенная обертка для библиотеки HLK-LD2450 с фильтрацией, трекингом целей и сглаживанием данных.

📋 Содержание
Особенности

Установка

Подключение

Быстрый старт

Справочник API

Инициализация

Обновление данных

Конфигурация

Получение данных

Отладка и служебные функции

Примеры

Настройка параметров

Типичные сценарии

Устранение неполадок

✨ Особенности
✅ Экспоненциальное сглаживание координат целей

✅ Устойчивый трекинг целей с уникальными ID

✅ Расчет скорости в м/с и км/ч

✅ Настраиваемые ограничения по углу и дистанции

✅ Сортировка целей по расстоянию (ближайшие первые)

✅ Конфигурируемый коэффициент сглаживания

✅ Отладочная информация и статистика

✅ Совместимость с ESP32 и ESP8266

📦 Установка
Arduino IDE
Установите библиотеку HLK-LD2450 через Менеджер библиотек

Скачайте Radar2450 как ZIP-архив

Скетч → Подключить библиотеку → Добавить .ZIP библиотеку

PlatformIO
Добавьте в platformio.ini:

ini
lib_deps = 
    Radar2450
    HLK-LD2450
🔌 Подключение
text
LD2450    ESP32
TX    →   GPIO16 (RADAR_RX)
RX    →   GPIO17 (RADAR_TX)
3.3V  →   3.3V
GND   →   GND
Важно: LD2450 работает на 3.3V логике!

🚀 Быстрый старт
cpp
#include <Radar2450.h>

Radar2450 radar;

void setup() {
    Serial.begin(115200);
    radar.begin(16, 17);           // Инициализация радара
    radar.setMaxDistance(6.0);     // Макс. дистанция 6 метров
    radar.setSmoothingFactor(0.2); // Коэффициент сглаживания
}

void loop() {
    radar.update();                // Обновление данных
    
    if (radar.getActiveTargetCount() > 0) {
        float dist = radar.getTargetDistance(1);   // Дистанция до ближайшей цели
        float angle = radar.getTargetAngle(1);     // Угол цели
        float speed = radar.getTargetSpeedKmh(1);  // Скорость в км/ч
        
        Serial.printf("Цель: %.1fм, %.0f°, %.1fкм/ч\n", dist, angle, speed);
    }
    
    delay(50);  // 20 Гц - оптимальная частота обновления
}
📚 Справочник API
Инициализация
Radar2450()
Конструктор класса. Создает новый экземпляр радара.

cpp
Radar2450 radar;  // Создание объекта радара
bool begin(int rx_pin, int tx_pin, bool debug = false)
Инициализация радара с указанием пинов для ESP32.

cpp
bool success = radar.begin(16, 17);  // RX=16, TX=17
Параметры:

rx_pin - GPIO для приема данных от радара (TX радара → RX ESP32)

tx_pin - GPIO для передачи данных к радару (RX радара → TX ESP32)

debug - вывод отладочных сообщений (по умолчанию: false)

Возвращает: true при успешной инициализации, false при ошибке

bool begin(HardwareSerial& serial_port, bool debug = false)
Инициализация с использованием готового объекта HardwareSerial.

cpp
Serial1.begin(256000, SERIAL_8N1, 16, 17);
bool success = radar.begin(Serial1);  // Использование Serial1
Обновление данных
int update()
Обновление данных радара. Должен вызываться в основном цикле программы.

cpp
int detected = radar.update();  // detected = количество обнаруженных целей
Возвращает: Количество целей, обнаруженных в текущем цикле (0-3)

Рекомендация: Вызывать с задержкой 50 мс (20 Гц) для оптимальной работы.

Конфигурация
void setMaxDistance(float max_distance_m)
Установка максимальной дистанции обнаружения.

cpp
radar.setMaxDistance(8.0);  // Макс. дистанция 8 метров
Диапазон: 0.1 - 10.0 метров
По умолчанию: 8.0 метров

void setSmoothingFactor(float alpha)
Установка коэффициента экспоненциального сглаживания.

cpp
radar.setSmoothingFactor(0.15);  // Среднее сглаживание
Диапазон: 0.01 - 0.99
По умолчанию: 0.15
Формула: новое_значение = alpha * сырое_значение + (1-alpha) * предыдущее

Значение	Эффект	Применение
0.01-0.1	Сильное сглаживание	Стабильное отслеживание
0.1-0.3	Среднее сглаживание	Общее применение
0.3-0.5	Легкое сглаживание	Быстрое реагирование
0.5-0.99	Минимальное сглаживание	Высокая точность
void setMaxTargets(int max_targets)
Установка максимального количества целей для отслеживания.

cpp
radar.setMaxTargets(5);  // Отслеживать до 5 целей
Диапазон: 1 - 10
По умолчанию: 10

void setAngleLimits(float min_angle, float max_angle)
Установка ограничений по углу обзора.

cpp
radar.setAngleLimits(-45, 45);  // Угол обзора от -45° до 45°
Диапазон: -90° - 90°
По умолчанию: -60° - 60°

Получение данных
int getActiveTargetCount()
Получение количества активных (отслеживаемых) целей.

cpp
int active = radar.getActiveTargetCount();  // active = 0, 1, 2, 3...
Возвращает: Количество активных целей (0 - maxTargets)

int getTargetID(int target_index)
Получение уникального ID цели. Цели сортируются по дистанции.

cpp
int id = radar.getTargetID(1);  // ID ближайшей цели
Параметры:

target_index - индекс цели (1 = ближайшая, 2 = следующая, и т.д.)

Возвращает: Уникальный ID цели или -1 если цель не найдена

float getTargetDistance(int target_index)
Получение дистанции до цели в метрах.

cpp
float distance = radar.getTargetDistance(1);  // distance = 2.5 (метра)
Возвращает: Дистанция в метрах или 0 если цель не найдена

float getTargetAngle(int target_index)
Получение угла цели в градусах.

cpp
float angle = radar.getTargetAngle(1);  // angle = -15.3 (градуса)
Возвращает: Угол в градусах (-180° - 180°) или 0 если цель не найдена

Направление углов:

Отрицательные значения (-) - слева от радара

Положительные значения (+) - справа от радара

0° - прямо перед радаром

float getTargetX(int target_index)
Получение координаты X цели в метрах.

cpp
float x = radar.getTargetX(1);  // x = 0.5 (метра)
Возвращает: Координата X в метрах (поперечное направление)

float getTargetY(int target_index)
Получение координаты Y цели в метрах.

cpp
float y = radar.getTargetY(1);  // y = 2.0 (метра)
Возвращает: Координата Y в метрах (продольное направление)

Система координат:

Радар находится в точке (0, 0)

Ось Y - направление вперед от радара

Ось X - направление влево/вправо

float getTargetSpeed(int target_index)
Получение скорости цели в метрах в секунду.

cpp
float speed = radar.getTargetSpeed(1);  // speed = 0.5 (м/с)
Возвращает: Скорость в м/с или 0 если цель не найдена

float getTargetSpeedKmh(int target_index)
Получение скорости цели в километрах в час.

cpp
float speedKmh = radar.getTargetSpeedKmh(1);  // speedKmh = 1.8 (км/ч)
Возвращает: Скорость в км/ч или 0 если цель не найдена

bool getTargetMoving(int target_index)
Проверка, движется ли цель.

cpp
bool moving = radar.getTargetMoving(1);  // moving = true/false
Возвращает: true если цель движется, false если неподвижна

Отладка и служебные функции
void printDebugInfo(Stream& stream = Serial)
Вывод подробной отладочной информации.

cpp
radar.printDebugInfo();  // Вывод в Serial
radar.printDebugInfo(Serial2);  // Вывод в Serial2
Пример вывода:

text
Radar2450 - Update Rate: 19.5 Hz
Active Targets: 2/10
  Slot 0: ID=1, Dist=2.35m, Angle=-15.2°, Pos=(0.61,2.27)m, Speed=0.42m/s
  Slot 1: ID=2, Dist=3.81m, Angle=28.7°, Pos=(1.83,3.33)m, Speed=0.18m/s
float getUpdateRateHz()
Получение текущей частоты обновления данных.

cpp
float rate = radar.getUpdateRateHz();  // rate = 19.8 (Гц)
Возвращает: Частоту обновления в Герцах

void reset()
Сброс всех целей и счетчиков.

cpp
radar.reset();  // Сброс всех данных
LD2450* getRadar()
Получение указателя на базовый объект LD2450 (для расширенного использования).

cpp
LD2450* rawRadar = radar.getRadar();  // Прямой доступ к библиотеке HLK-LD2450
📖 Примеры
Пример 1: Простое обнаружение движения
cpp
#include <Radar2450.h>
Radar2450 radar;

void setup() {
    Serial.begin(115200);
    radar.begin(16, 17);
}

void loop() {
    radar.update();
    
    if (radar.getActiveTargetCount() > 0) {
        Serial.println("Движение обнаружено!");
        Serial.printf("Дистанция: %.1fм\n", radar.getTargetDistance(1));
    }
    
    delay(50);
}
Пример 2: Отслеживание скорости
cpp
#include <Radar2450.h>
Radar2450 radar;

void setup() {
    Serial.begin(115200);
    radar.begin(16, 17);
}

void loop() {
    radar.update();
    
    if (radar.getTargetMoving(1)) {
        float speedKmh = radar.getTargetSpeedKmh(1);
        if (speedKmh > 5.0) {
            Serial.println("Быстрое движение!");
        }
    }
    
    delay(50);
}
Пример 3: Множественные цели
cpp
#include <Radar2450.h>
Radar2450 radar;

void setup() {
    Serial.begin(115200);
    radar.begin(16, 17);
    radar.setMaxTargets(3);
}

void loop() {
    radar.update();
    
    int active = radar.getActiveTargetCount();
    for (int i = 1; i <= active; i++) {
        Serial.printf("Цель %d: ID=%d, Dist=%.1fм, Angle=%.0f°, Speed=%.1fкм/ч\n",
                     i,
                     radar.getTargetID(i),
                     radar.getTargetDistance(i),
                     radar.getTargetAngle(i),
                     radar.getTargetSpeedKmh(i));
    }
    
    delay(50);
}
⚙️ Настройка параметров
Рекомендуемые настройки для разных сценариев:
Обнаружение присутствия
cpp
radar.setSmoothingFactor(0.1);    // Сильное сглаживание
radar.setMaxDistance(6.0);        // Дистанция до 6 метров
radar.setAngleLimits(-60, 60);    // Широкий угол обзора
Точное отслеживание
cpp
radar.setSmoothingFactor(0.3);    // Умеренное сглаживание
radar.setMaxDistance(4.0);        // Короткая дистанция
radar.setAngleLimits(-30, 30);    // Узкий угол для точности
Быстрое реагирование
cpp
radar.setSmoothingFactor(0.5);    // Минимальное сглаживание
radar.setMaxDistance(8.0);        // Дальнее обнаружение
radar.setAngleLimits(-90, 90);    // Полный обзор
🎯 Типичные сценарии
1. Управление светом
cpp
if (radar.getActiveTargetCount() > 0 && radar.getTargetDistance(1) < 3.0) {
    digitalWrite(LED_PIN, HIGH);  // Включить свет
} else {
    digitalWrite(LED_PIN, LOW);   // Выключить свет
}
2. Охранная система
cpp
if (radar.getTargetMoving(1) && radar.getTargetSpeedKmh(1) > 2.0) {
    triggerAlarm();  // Срабатывание сигнализации
}
3. Подсчет людей
cpp
if (radar.getActiveTargetCount() > previousCount) {
    peopleCount++;  // Увеличить счетчик
}
previousCount = radar.getActiveTargetCount();
4. Направленное управление
cpp
float angle = radar.getTargetAngle(1);
if (angle < -30) {
    turnLeft();   // Цель слева
} else if (angle > 30) {
    turnRight();  // Цель справа
} else {
    moveForward(); // Цель прямо
}
🔧 Устранение неполадок
Ошибка: Radar2450 does not name a type
text
Решение: 
1. Убедитесь, что папка библиотеки называется точно "Radar2450"
2. Проверьте наличие файлов Radar2450.h и Radar2450.cpp
3. Перезагрузите Arduino IDE
Нет данных с радара
text
Решение:
1. Проверьте подключение: TX радара → RX ESP32, RX радара → TX ESP32
2. Убедитесь, что радар питается от 3.3V
3. Проверьте скорость UART: 256000 бод
4. Вызовите radar.begin() с debug=true для диагностики
Нестабильные показания
text
Решение:
1. Увеличьте коэффициент сглаживания: setSmoothingFactor(0.1)
2. Установите ограничения по углу: setAngleLimits(-45, 45)
3. Убедитесь, что нет помех от других устройств
Низкая частота обновления
text
Решение:
1. Уменьшите задержку в loop(): delay(30) вместо delay(100)
2. Упростите код вывода в Serial
3. Убедитесь, что не вызываются медленные функции
📊 Технические характеристики
Параметр	Значение	Описание
Частота обновления	~20 Гц	Рекомендуемая 20 Гц (delay 50ms)
Макс. количество целей	10	Одновременно отслеживаемых
Диапазон дистанций	0.1-10 м	Настраивается setMaxDistance()
Точность дистанции	±0.1 м	В стабильных условиях
Точность угла	±2°	В стабильных условиях
Потребление памяти	~500 байт	Для структуры целей
Совместимость	ESP32, ESP8266	ESP32 рекомендуется
📞 Поддержка
Полезные ссылки
Документация HLK-LD2450

Датчик LD2450 на AliExpress

Форум Arduino

Часто задаваемые вопросы
Q: Можно ли использовать с другими микроконтроллерами?
A: Да, но требуется поддержка HardwareSerial на скорости 256000 бод.

Q: Как увеличить дальность обнаружения?
A: Фактическая дальность ограничена датчиком (~8-10м). Убедитесь в отсутствии помех.

Q: Почему цели иногда "прыгают"?
A: Используйте большее сглаживание: setSmoothingFactor(0.1)

Q: Как откалибровать радар?
A: Калибровка не требуется, но можно настроить setAngleLimits() под ваше размещение.

📄 Лицензия
MIT License. Свободное использование с указанием авторства.# Radar2450-Library

# LD2450radar-Library

Расширенная обертка для библиотеки HLK-LD2450 с фильтрацией, трекингом целей и сглаживанием данных.

**Репозиторий:** https://github.com/eu1abg/LD2450radar-Library.git

## 📋 Требования

- **Платформы:** ESP32 (рекомендуется), другие платформы с HardwareSerial
- **Зависимость:** [HLK-LD245X библиотека v0.9.1](https://github.com/Fiooodooor/HLK-LD245X.git)
- **Среда:** Arduino IDE или PlatformIO

## 🚀 Установка

### Arduino IDE
1. Установите [HLK-LD245X v0.9.1](https://github.com/Fiooodooor/HLK-LD245X.git)
2. Установите LD2450radar-Library:
   - Скачайте ZIP с https://github.com/eu1abg/LD2450radar-Library.git
   - В Arduino IDE: Скетч → Подключить библиотеку → Добавить .ZIP библиотеку...

### PlatformIO
Добавьте в `platformio.ini`:
```ini
lib_deps = 
    https://github.com/Fiooodooor/HLK-LD245X.git#0.9.1
    https://github.com/eu1abg/LD2450radar-Library.git
```
## 🔧 Основные возможности




```text
✅ Экспоненциальное сглаживание координат целей (EMA фильтр)

✅ Устойчивый трекинг целей с уникальными ID

✅ Расчет скорости в м/с на основе изменения дистанции

✅ Настраиваемые параметры: коэффициент сглаживания, макс. дистанция

✅ Двойной API: доступ по активным целям и по сырым индексам

✅ Совместимость с ESP32 и другими платформами с HardwareSerial
```




## ТРЕБОВАНИЯ К ОБОРУДОВАНИЮ

Библиотека `LD2450radar` является расширением для библиотеки `HLK-LD2450` и требует ее установки. Датчик `HLK-LD2450` работает на частоте 24 ГГц и использует последовательное соединение на скорости 256000 бод. Рекомендуется использовать микроконтроллеры с аппаратным UART для стабильной работы.

##  📊 Технические характеристики

```text 
Параметр	                	 Значение	                     Описание
Частота обновления	         	 ~20 Гц	                Рекомендуемая 20 Гц (delay 50ms)
Макс. целей (по умолчанию)	 	   3	                        Настраивается в конструкторе
Диапазон дистанций	        	  0.0 - configurable	        Настраивается maxDistM
Коэффициент сглаживания	         	 0.0-1.0	                Экспоненциальное сглаживание
Потребление памяти	         	 ~200 байт/цель	         Динамическое выделение
Скорость UART	                	  256000 бод	                 По умолчанию
```


  Совместимость	ESP32, ESP8266	ESP32 рекомендуется

## ⚙️ Конфигурация

### Конструктор

```cpp
LD2450radar(HardwareSerial &serial, int rxPin, int txPin, 
            unsigned long baud = 256000, int maxTargets = 10,
            float alpha = 0.1f, float maxDistM = 10.0f);

```




## ⚙️ Настройка параметров

### Рекомендуемые настройки для разных сценариев:


#### Настройка для охранной системы

```cpp
LD2450radar radar(
    Serial1,      // Используем Serial1
    16, 17,       // RX=16, TX=17
    256000,       // Скорость UART
    3,            // Отслеживаем только 3 цели
    0.2f,         // Умеренное сглаживание
    5.0f          // Обнаружение до 5 метров
);

```
#### Точное отслеживание
```cpp
LD2450radar radar(
    Serial2,      // Используем Serial2
    32, 33,       // Другие пины UART
    256000,       // Скорость UART
    1,            // Только 1 цель (ближайшая)
    0.3f,         // Легкое сглаживание
    4.0f          // Короткая дистанция
);
```
#### Быстрое реагирование
```cpp
// Оптимальная конфигурация для минимальной задержки
LD2450radar radar(
    Serial1,      // Используем первый UART (самый быстрый)
    16, 17,       // RX=16, TX=17 (стандартные пины ESP32)
    256000,       // Максимальная скорость UART
    3,            // Только 3 цели (уменьшаем обработку)
    0.6f,         // МИНИМАЛЬНОЕ сглаживание (быстрый отклик)
    6.0f          // Средняя дистанция
);
```
#### 🔧 Дополнительные оптимизации

```cpp
// 1. Используйте прерывания для детектирования движения
volatile bool motionDetected = false;

// 2. Включите оптимизацию компилятора
// В PlatformIO: build_flags = -O3
// В Arduino: Меню → Инструменты → Оптимизация → "Быстрее"

// 3. Используйте прямые регистры для вывода
if (radar.getDistance(1) < 1.0f) {
    GPIO.out_w1ts = (1 << LED_PIN); // Включить LED мгновенно
}

// 4. Кэшируйте часто используемые данные
static float lastDistance = 0;
float current = radar.getDistance(1);
if (abs(current - lastDistance) > 0.1f) {
    // Только если значительное изменение
    processMovement();
    lastDistance = current;
}
```


# 🎯 Примеры использования


## 1. Базовый пример
```cpp
#include <LD2450radar.h>

LD2450radar radar(Serial1, 16, 17); // RX=16, TX=17 (ESP32)

void setup() {
    Serial.begin(115200);
    radar.begin(); // Инициализация
}

void loop() {
    radar.update(); // Обновление данных
    
    int active = radar.getActiveCount();
    if (active > 0) {
        Serial.print("Активных целей: ");
        Serial.println(active);
        
        // Данные первой активной цели
        Serial.print("Дистанция: ");
        Serial.print(radar.getDistance(1));
        Serial.print(" м, Скорость: ");
        Serial.print(radar.getSpeed(1));
        Serial.println(" м/с");
    }
    delay(50); // ~20 Гц
}
```




### 2. Управление светом
```cpp
if (radar.getActiveCount() > 0 && radar.getDistance(1) < 3.0) {
    digitalWrite(LED_PIN, HIGH);  // Включить свет
} else {
    digitalWrite(LED_PIN, LOW);   // Выключить свет
}
```
### 3. Охранная система
```cpp
if (radar.getTargetMoving(1) && radar.getTargetSpeedKmh(1) > 2.0) {
    triggerAlarm();  // Срабатывание сигнализации
}
```

### 4. Направленное управление
```cpp
#include <LD2450radar.h>

LD2450radar radar(Serial1, 16, 17, 256000, 2, 0.2f, 4.0f);

// Счётчики
int peopleIn = 0;    // Вошли
int peopleOut = 0;   // Вышли
int totalInside = 0; // Сейчас внутри

// Пороги
#define ENTER_THRESHOLD 0.7f   // Порог входа
#define EXIT_THRESHOLD 2.3f    // Порог выхода
#define MIN_SPEED 0.2f         // Минимальная скорость для учета

// Трекер положения
float lastX = 0, lastY = 0;
unsigned long lastUpdate = 0;
bool wasInside = false;

void setup() {
    Serial.begin(115200);
    radar.begin();
    pinMode(LED_IN_PIN, OUTPUT);
    pinMode(LED_OUT_PIN, OUTPUT);
    Serial.println("Система подсчета с направлением");
}

void loop() {
    radar.update();
    
    if (radar.getActiveCount() > 0) {
        float x = radar.getX(1);
        float y = radar.getY(1);
        float speed = radar.getSpeed(1);
        
        // Расстояние до радара
        float distance = sqrt(x*x + y*y);
        
        // Определяем положение относительно порогов
        bool isInside = distance < ENTER_THRESHOLD;
        bool isOutside = distance > EXIT_THRESHOLD;
        
        // Определение направления по изменению координат
        if (lastUpdate > 0 && millis() - lastUpdate < 100) {
            float dx = x - lastX;
            float dy = y - lastY;
            
            // Приближение (dy отрицательно при движении вперед от радара)
            // В нашей системе Y - направление вперед от радара
            if (abs(speed) > MIN_SPEED) {
                if (!wasInside && isInside && dy < 0) {
                    // ВХОД: был снаружи, теперь внутри, движется "вглубь"
                    peopleIn++;
                    totalInside++;
                    Serial.printf("ВХОД! Вошли: %d, Сейчас внутри: %d\n", 
                                 peopleIn, totalInside);
                    digitalWrite(LED_IN_PIN, HIGH);
                    delay(300);
                    digitalWrite(LED_IN_PIN, LOW);
                    wasInside = true;
                }
                else if (wasInside && isOutside && dy > 0) {
                    // ВЫХОД: был внутри, теперь снаружи, движется "наружу"
                    peopleOut++;
                    if (totalInside > 0) totalInside--;
                    Serial.printf("ВЫХОД! Вышли: %d, Сейчас внутри: %d\n", 
                                 peopleOut, totalInside);
                    digitalWrite(LED_OUT_PIN, HIGH);
                    delay(300);
                    digitalWrite(LED_OUT_PIN, LOW);
                    wasInside = false;
                }
            }
        }
        
        // Сохраняем для следующего сравнения
        lastX = x;
        lastY = y;
        lastUpdate = millis();
    }
    
    // Периодический вывод статистики
    static unsigned long lastReport = 0;
    if (millis() - lastReport > 5000) {
        Serial.println("=== СТАТИСТИКА ===");
        Serial.printf("Вошли: %d\n", peopleIn);
        Serial.printf("Вышли: %d\n", peopleOut);
        Serial.printf("Сейчас внутри: %d\n", totalInside);
        Serial.printf("Баланс: %d\n", peopleIn - peopleOut);
        Serial.println("================");
        lastReport = millis();
    }
    
    delay(50);
}
```

### 5. Подсчет проходящих людей
```cpp
#include <LD2450radar.h>

LD2450radar radar(Serial1, 16, 17, 256000, 5, 0.15f, 4.0f);

// Переменные для подсчёта
static int peopleCount = 0;
static float lastDistance = 0.0f;
static unsigned long lastDetectionTime = 0;

void setup() {
    Serial.begin(115200);
    radar.begin();
    Serial.println("Система подсчета людей запущена");
}

void loop() {
    radar.update();
    
    if (radar.getActiveCount() > 0) {
        float currentDistance = radar.getDistance(1); // Ближайшая цель
        
        // Логика обнаружения прохода:
        // 1. Сначала цель была далеко (>2м)
        // 2. Потом приблизилась близко (<1м)
        if (currentDistance < 1.0f && lastDistance > 2.0f) {
            // Защита от двойного срабатывания (дебаунс)
            if (millis() - lastDetectionTime > 2000) { // Не чаще 1 раза в 2 секунды
                peopleCount++;
                Serial.printf("Прошел человек! Всего: %d\n", peopleCount);
                lastDetectionTime = millis();
                
                // Можно добавить звуковой/световой сигнал
                digitalWrite(BUZZER_PIN, HIGH);
                delay(100);
                digitalWrite(BUZZER_PIN, LOW);
            }
        }
        
        lastDistance = currentDistance;
    }
    
    delay(50); // ~20 Гц
}
```
### 5.1 🎯 Улучшенная версия с зонами
```cpp #include <LD2450radar.h>

LD2450radar radar(Serial1, 16, 17, 256000, 3, 0.1f, 5.0f);

// Состояния для отслеживания
enum PersonState {
    OUTSIDE,    // Человек вне зоны
    ENTERING,   // Входит в зону
    INSIDE,     // Внутри зоны
    EXITING     // Выходит из зоны
};

PersonState currentState = OUTSIDE;
int peopleCount = 0;
unsigned long stateChangeTime = 0;

// Зоны обнаружения (в метрах)
#define FAR_ZONE 2.5f    // Дальняя зона
#define NEAR_ZONE 0.8f   // Ближняя зона
#define HYSTERESIS 0.2f  // Гистерезис для устранения дребезга

void setup() {
    Serial.begin(115200);
    radar.begin();
    Serial.println("Система подсчета людей (улучшенная)");
}

void loop() {
    radar.update();
    
    if (radar.getActiveCount() > 0) {
        float dist = radar.getDistance(1);
        float speed = radar.getSpeed(1);
        
        // Машина состояний для точного отслеживания
        switch (currentState) {
            case OUTSIDE:
                if (dist < FAR_ZONE && dist > NEAR_ZONE) {
                    currentState = ENTERING;
                    stateChangeTime = millis();
                    Serial.println("Обнаружено приближение...");
                }
                break;
                
            case ENTERING:
                if (dist <= NEAR_ZONE) {
                    currentState = INSIDE;
                    stateChangeTime = millis();
                } else if (dist > FAR_ZONE + HYSTERESIS) {
                    currentState = OUTSIDE; // Отошёл
                }
                break;
                
            case INSIDE:
                if (dist > NEAR_ZONE + HYSTERESIS) {
                    currentState = EXITING;
                    stateChangeTime = millis();
                }
                break;
                
            case EXITING:
                if (dist > FAR_ZONE) {
                    // УСПЕШНОЕ завершение прохода
                    peopleCount++;
                    Serial.printf("Человек прошел! Всего: %d\n", peopleCount);
                    currentState = OUTSIDE;
                    
                    // Визуальная индикация
                    blinkLED(3);
                } else if (dist < NEAR_ZONE - HYSTERESIS) {
                    // Вернулся обратно - отмена
                    currentState = INSIDE;
                    Serial.println("Отмена: человек вернулся");
                }
                break;
        }
        
        // Защита от зависаний (таймаут состояния)
        if (millis() - stateChangeTime > 10000) { // 10 секунд
            currentState = OUTSIDE;
        }
    }
    
    // Вывод текущего состояния каждые 2 секунды
    static unsigned long lastPrint = 0;
    if (millis() - lastPrint > 2000) {
        Serial.printf("Состояние: %d, Людей сегодня: %d\n", 
                     currentState, peopleCount);
        lastPrint = millis();
    }
    
    delay(40); // 25 Гц
}

void blinkLED(int times) {
    for (int i = 0; i < times; i++) {
        digitalWrite(LED_PIN, HIGH);
        delay(150);
        digitalWrite(LED_PIN, LOW);
        delay(150);
    }
}
```

### 6.⏱️ Пример кода с минимальной задержкой

```cpp
#include <LD2450radar.h>

// Конфигурация для максимальной скорости
LD2450radar radar(Serial1, 16, 17, 256000, 2, 0.7f, 5.0f);

void setup() {
    Serial.begin(115200);
    radar.begin();
}

void loop() {
    unsigned long start = micros(); // Замер времени
    
    int rawTargets = radar.update(); // Получаем данные
    
    unsigned long processTime = micros() - start;
    
    // БЫСТРЫЙ доступ к данным (без сортировки по активности)
    for (int i = 1; i <= radar.capacity(); i++) {
        if (radar.getIDRaw(i) != -1) { // Цель существует
            float dist = radar.getDistanceRaw(i);
            float speed = radar.getSpeedRaw(i);
            
            // Мгновенная реакция
            if (dist < 1.5f && abs(speed) > 0.3f) {
                triggerFastAction(); // Немедленная реакция
            }
        }
    }
    
    // Минимальная задержка для максимальной частоты
    delay(25); // ~40 Гц!
    
    // Отладочная информация
    if (processTime > 1000) {
        Serial.print("ВНИМАНИЕ: Задержка обработки: ");
        Serial.print(processTime);
        Serial.println(" мкс");
    }
}

void triggerFastAction() {
    // Мгновенная реакция на движение
    digitalWrite(ALARM_PIN, HIGH);
    delay(10); // Короткий импульс
    digitalWrite(ALARM_PIN, LOW);
}
```

### 7.🚀 Итоговый рецепт "Максимальная скорость"

```cpp
// АБСОЛЮТНЫЙ МИНИМУМ ЗАДЕРЖКИ
LD2450radar radar(Serial1, 16, 17, 256000, 1, 0.8f, 4.0f);

void setup() {
    Serial.begin(115200);
    radar.begin();
    // Настройка пинов на максимальную скорость
    pinMode(ACTION_PIN, OUTPUT);
    digitalWrite(ACTION_PIN, LOW);
}

void loop() {
    radar.update(); // Получаем данные
    
    // ПРЯМАЯ реакция на ближайшую цель
    float dist = radar.getDistanceRaw(1);
    
    if (dist > 0 && dist < 1.0f) { // Цель в пределах 1 метра
        GPIO.out_w1ts = (1 << ACTION_PIN); // МГНОВЕННОЕ действие
        delayMicroseconds(100); // Короткий импульс
        GPIO.out_w1tc = (1 << ACTION_PIN);
    }
    
    delay(20); // 50 Гц - максимум для HLK-LD2450
}
```
  Результат: Задержка "датчик → действие" менее 25 мс!


## API

### Инициализация

Метод `begin` инициализирует радар.

```cpp
bool begin(bool configureMultiTarget = true)
```
### Основные методы
```cpp
int update() // - обновление данных, возвращает количество сырых целей
```
```cpp
int getActiveCount() // - количество активных целей (lostCounter == 0)
```


### Доступ к данным активных целей (1-based индекс)



```cpp 
int getID(int n) - ID n-й активной цели 
```
```cpp 
float getDistance(int n) - дистанция (метры) 
```
```cpp 
float getAngle(int n) - угол (градусы)
```
```cpp 
float getX(int n) - координата X (метры)
 ```

```cpp float getY(int n) - координата Y (метры)

```

```cpp float getSpeed(int n) - скорость (м/с)

```

### Сырой доступ по индексам (1..capacity())

```cpp int getIDRaw(int idx) - ID по индексу 
```
```cpp float getDistanceRaw(int idx) - дистанция по индексу 
```
```cpp float getAngleRaw(int idx) - угол по индексу 
```
```cpp float getXRaw(int idx) - координата X по индексу 
```
```cpp float getYRaw(int idx) - координата Y по индексу 
```
```cpp float getSpeedRaw(int idx) - скорость по индексу 
```


### Вспомогательные методы

```cpp int capacity() - максимальное количество отслеживаемых целей
 ```
```cpp void reset() - сброс всех целей 
```





# 🎛️ Настройка параметров



## Коэффициент сглаживания (alpha)



```cpp 0.0 - нет сглаживания (сырые данные) 
```
```cpp 0.1 - сильное сглаживание (плавное движение) 
```
```cpp 0.3 - умеренное сглаживание 
```
```cpp 0.5 - легкое сглаживание (быстрый отклик) 
```


## Максимальная дистанция (maxDistM)


   Цели дальше указанной дистанции игнорируются (обрезка на уровне кода).




# 🐛 Отладка

    Для отладки используйте сырой доступ к данным:


```cpp // Вывод всех слотов (активных и неактивных)
for (int i = 1; i <= radar.capacity(); i++) {
    if (radar.getIDRaw(i) != -1) {
        Serial.printf("Слот %d: ID=%d, Дистанция=%.2fм\n", 
                      i, radar.getIDRaw(i), radar.getDistanceRaw(i));
    }
} ```


### ПОДКЛЮЧЕНИЕ

Подключите датчик HLK-LD2450 к ESP32 согласно следующей схеме:
```markdown

HLK-LD2450 ESP32
    TX →   GPIO16
    RX →   GPIO17
    3.3V → 3.3V
    GND →  GND
```
**Примечание:** Убедитесь, что вы используете правильные пины UART на вашей плате ESP32. В примере используются GPIO16 (RX2) и GPIO17 (TX2) — второй аппаратный UART ESP32.





# 📄 Структура проекта


```markdown 


LD2450radar-Library/
├── src/
│   ├── LD2450radar.h    # Основной заголовочный файл
│   └── LD2450radar.cpp  # Реализация
├── examples/
│   └── Basic/
│       └── Basic.ino    # Пример использования
├── library.properties    # Конфигурация Arduino IDE
├── library.json         # Конфигурация PlatformIO
├── LICENSE              # Лицензия MIT
└── README.md            # Эта документация ```








## 📞 Поддержка

### Полезные ссылки

[Документация HLK-LD2450](https://github.com/Fiooodooor/HLK-LD245X)

[Исходный код библиотеки](https://github.com/eu1abg/LD2450radar-Library)

[Arduino Forum](https://forum.arduino.cc)


## 📄 Лицензия

MIT License. Подробности в файле LICENSE.




